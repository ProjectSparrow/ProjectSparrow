///scr_axis_direction_v(button, device, device_type, state, value)

var button = argument0;
var device = argument1;
var device_type = argument2;
var state = argument3;
var value = argument4;

if (device == device_id)
{
    point_y = value;
    direction_changed = true;
}

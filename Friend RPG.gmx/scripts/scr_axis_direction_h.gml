///scr_axis_direction_h(button, device, device_type, state, value)

var button = argument0;
var device = argument1;
var device_type = argument2;
var state = argument3;
var value = argument4;

if (device == device_id)
{
    point_x = value;
    direction_changed = true;
}
